import Main from './main';
import Admin from './admin';
import Profile from './profile';
import Inv from './inv';

export default {
  main: Main,
  admin: Admin,
  profile: Profile,
  inventory: Inv
}
